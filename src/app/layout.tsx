import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/toaster";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Tazo Art Studio — Create. Collect. Battle.",
  description: "AI-powered Tazo Art Creator for Trading Tazos Game. Generate unique tazo art across Minimon, Dracobell & Cybermon collections with 90s magazine aesthetic.",
  keywords: ["tazo", "art", "generator", "AI", "minimon", "dracobell", "cybermon", "trading", "collectible"],
  authors: [{ name: "Tazo Art Studio" }],
  icons: {
    icon: "/logo.svg",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
        <Toaster />
      </body>
    </html>
  );
}
